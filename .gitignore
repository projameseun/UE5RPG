# [UE5 소스코드 전용 최적화 설정]

# --- 1. 에디터 및 컴파일러 생성 파일 (필수 제외) ---
.vs/
.vscode/
.idea/
*.sln
*.suo
*.opensdf
*.sdf
*.VC.db
*.VC.opendb
.vsconfig

# --- 2. 컴파일 및 빌드 결과물 (절대 금지) ---
Binaries/
Plugins/**/Binaries/
Build/
Intermediate/
Plugins/**/Intermediate/
DerivedDataCache/

# --- 3. 로컬 설정 및 임시 저장 데이터 (제외) ---
Saved/*
# 프로젝트 환경설정(Config)은 소스코드에 포함되어야 하므로 아래처럼 Saved 내의 로그/캐시만 제외하거나 
# Saved 전체를 제외하는 것이 일반적입니다. (Config 폴더는 루트에 따로 존재함)

# --- 4. 바이너리 오브젝트 및 라이브러리 ---
*.obj
*.pdb
*.user
*.aps
*.pch
*.vspscc
*.vssscc
*.suo
*.bak
*.log
*.tlog
*.lastbuildstate
*.exe
*.dll
*.lib
*.so
*.dylib

# --- 5. 맵 빌드 데이터 (소스코드 외 데이터) ---
# 라이팅 빌드 및 내비게이션 데이터는 용량이 크므로 제외
*_BuiltData.uasset

# --- 6. 기타 언리얼 생성 임시 파일 ---
.ignore
FileOpenOrder/

# --- 7. 소스 아트 (선택 사항) ---
# 원본 PSD나 TGA 등 소스코드가 아닌 리소스 파일 제외
SourceArt/